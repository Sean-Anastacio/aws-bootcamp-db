<h1>ユーザー登録</h1>

<%= form_with(model: @user, local: true) do |form| %>
  <div>
    <%= form.label :name, "名前" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit "登録する" %>
  </div>
<% end %>

<%= link_to '戻る', users_path %>
